# Part 2 environment. Copy to `.env` in this folder for `docker compose` variable substitution.

# Gemini (required for POST /ingest in the API container)
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Optional — local `uvicorn api.main:app` without Docker
# MQTT_MERGE_URL=http://127.0.0.1:8000
