# Required for live Gemini calls (CLI, dashboard drag-and-drop OCR, API POST /ingest)
GEMINI_API_KEY=
# GOOGLE_API_KEY=

GEMINI_MODEL=gemini-flash-latest

# Max PDF pages to render per file (cost / latency control)
GEMINI_MAX_PAGES=8
